{
  "name": "appstore-redirect-firebase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "firebase emulators:start --only hosting,functions",
    "deploy": "firebase deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "firebase-tools": "^13.0.0",
    "vitest": "^1.6.0"
  }
}
