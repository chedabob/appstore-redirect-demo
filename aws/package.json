{
  "name": "appstore-redirect-aws",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node tools/local-server.js",
    "deploy": "node tools/deploy.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "vitest": "^1.6.0"
  }
}
